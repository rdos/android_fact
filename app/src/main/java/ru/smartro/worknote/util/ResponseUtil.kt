package ru.smartro.worknote.util

object ResponseUtil {

  /*  fun wayTaskToEntity(response: WayInfo): WayTaskEntity {
*//*        val start = WayStartPointEntity(coordinates = RealmList(response.start.coordinates[0], response.start.coordinates[1]))
        val unload = WayUnloadPointEntity()
        val entity = WayTaskEntity(id = response.id, accounting = response.accounting, beginnedAt = response.beginnedAt, finishedAt = response.finishedAt,
        name = response.name, points = , start = start, unload = )*//*
    }*/
}